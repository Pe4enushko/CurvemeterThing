using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapBehaviour : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //var A = transform.Find("A");
        //var B = transform.Find("B");

        //Vector2 AFlat = new(A.position.x, A.position.z);
        //Vector2 BFlat = new(B.position.x, B.position.z);

        //var koefUnitsToKilometers = 18.0849904124;

        //float distance = Mathf.Abs(Vector2.Distance(AFlat, BFlat));

        //print("distance = " + distance);
        //print("расстояние между точками на местности: " + distance * koefUnitsToKilometers + " км");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
